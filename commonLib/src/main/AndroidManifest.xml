<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.aorise.common">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <application>
        <!-- 登录后跳转的首页 start-->
        <!--        <meta-data
                    android:name="PACKAGE_NAME_KEY"
                    android:value="${PACKAGE_NAME_VALUE}"/>
                <meta-data
                    android:name="CLASS_NAME_KEY"
                    android:value="${CLASS_NAME_VALUE}"/>-->
        <!-- 登录后跳转的首页 start-->

        <activity
            android:name="cn.aorise.common.component.login.AoriseSplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cn.aorise.common.component.login.AoriseLoginActivity"
            android:label="@string/aorise_component_title_activity_login"></activity>
    </application>
</manifest>
